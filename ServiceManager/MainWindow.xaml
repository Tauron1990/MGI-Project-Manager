<Window x:Class="ServiceManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceManager"
        mc:Ignorable="d" Loaded="MainWindow_OnLoaded"
        Title="Service Manager" Height="450" Width="800" IsEnabled="{Binding IsReady}">
    <Window.DataContext>
        <local:MainWindowsModel/>
    </Window.DataContext>
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
            <Button Content="Installieren" Margin="5"/>
            <Button Content="Deistallieren" Margin="5"/>
        </StackPanel>
        <ListBox Margin="5">

        </ListBox>
    </DockPanel>
</Window>
