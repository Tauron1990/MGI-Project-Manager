<catel:UserControl x:Class="Tauron.Application.Deployment.AutoUpload.Views.Common.CommonFinishView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:Tauron.Application.Deployment.AutoUpload.ViewModels.Common"
                   xmlns:catel="http://schemas.catelproject.com"
                   mc:Ignorable="d" Background="White" d:DataContext="{d:DesignInstance local:CommonFinishViewModel}"
                   d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="2*" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="Fertig" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center"
                   Text="{Binding Message}" FontSize="20" />
        <Button Grid.Row="2" Content="Fertig" MinWidth="100" VerticalAlignment="Center" HorizontalAlignment="Center"
                FontSize="20" Command="{Binding ReturnCommand}" />
    </Grid>
</catel:UserControl>